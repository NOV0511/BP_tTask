using System;
using System.Collections.Generic;
using System.Text;

namespace tTask.ORM
{
    public enum Roles
    {
		DomainAdmin = 1,
		NormalUser = 2,
		Manager = 3,
		Developer = 4,
		ProjectUser = 5,
		ProjectLeader = 6,
		ProjectRequest = 7
    }
}
